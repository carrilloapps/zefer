# Secret for strict encryption mode.
# Set this in your hosting environment (Vercel, Docker, etc.), NEVER commit it.
# If empty, strict encryption is disabled.
#
# Generate: openssl rand -hex 32
# This value must NEVER change once files have been encrypted with strict mode.
ZEFER_INSTANCE_SECRET=
